#include<bits/stdc++.h>
#define ll      long long
using namespace std;   
int main() 
{
	#ifndef qubais_judge
	freopen("input.txt","r",stdin);
	freopen("output.txt","w",stdout);
	#endif
	ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    int n;
    cin>>n;
    vector<int> v(5);
    for(int i=0;i<n;i++){
       int a;
       v.push_back(a);
    }
    int res=0;
    for(auto it=v.begin();it!=v.end()-1;i++){
     if(){

     }
    }



    cout<<res<<endl;
    return 0;
}